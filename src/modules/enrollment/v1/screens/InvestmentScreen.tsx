/**
 * V1 investment step — UI structure from Figma `investment-strategy.tsx` with Zustand wiring
 * in `InvestmentStrategy` (shared helpers + build-portfolio modal).
 */
import { InvestmentStrategy } from "./InvestmentStrategy";

export default InvestmentStrategy;
