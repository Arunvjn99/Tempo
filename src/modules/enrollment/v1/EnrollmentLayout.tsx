/**
 * Router imports `EnrollmentV1Layout` from `./layout/EnrollmentLayout.tsx`.
 * Re-export only — edit wizard shell and `SCREENS` there.
 */
export { EnrollmentV1Layout } from "./layout/EnrollmentLayout";
