/**
 * Company branding from Supabase (`companies.branding_json`, column colors) is optional.
 * Set to `true` only when API theme JSON is validated end-to-end.
 */
export const APPLY_SUPABASE_COMPANY_BRANDING = false;
