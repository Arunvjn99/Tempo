/**
 * Single gateway for the `framer-motion` package. App features/pages import
 * `motion`, `AnimatePresence`, and hooks from `@/ui/animations` only.
 */
export { AnimatePresence, motion, useReducedMotion } from "framer-motion";
export type { Easing, HTMLMotionProps, Transition } from "framer-motion";
